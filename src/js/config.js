// Here will be all viables used many times at different places in the app. We add them here to avoid making changes all over the places in cases that variables changes.
export const API_URL = `https://forkify-api.herokuapp.com/api/v2/recipes/`;

// It a common practice to write constant in UPPERCASE within the config files since these wont be changed in the future.
export const TIMEOUT_SEC = 10;
